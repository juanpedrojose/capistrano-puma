#!/bin/bash
app=$1;
cd $app || exit 1

if [ -e Gemfile ]; then
  exec sh -c "exec bundle exec puma -C <%= fetch(:puma_conf) %> --daemon"
else
  exec sh -c "exec puma -C <%= fetch(:puma_conf) %> --daemon"
fi
